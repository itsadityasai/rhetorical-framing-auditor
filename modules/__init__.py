"""Compatibility package for shared project modules.

This package re-exports implementations from pipeline.modules so scripts can
import `modules.*` from the repository root after project reorganization.
"""
